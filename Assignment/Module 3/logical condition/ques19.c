// program in C to calculate and print the electricity bill of a given
// customer. The customer ID, name, and unit consumed by the user should
// be captured from the keyboard to display the total amount to be paid to the
// customer.
#include<stdio.h>
int main()
{
    int id,name,units;
    printf("Enter Your Customer ID : ");
    scanf("%d",&id);
    printf("Enter Your Customer Name : ");
    scanf("%d",&name);
    printf("Enter Your Units Consumed : ");
    scanf("%d",&units);

    if(units<=350)
    {
        printf("");
    }
    else if(units<=350)
    {
        printf("");
    }
    else if(units<600 && units>350)
    {
        printf("");
    }
    else if(units<800 && units>600)
    {
        printf("");
    }
    else
    {

    }
 

}